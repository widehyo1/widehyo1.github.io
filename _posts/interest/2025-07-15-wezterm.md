---
layout: post
title: wezterm 설치 및 환경설정 기록
subtitle: wezterm configuration in windows
tags: [wezterm, devtools]
comments: true
author: widehyo
---


## 설치
[wezterm install in window](https://wezterm.org/install/windows.html)

```
widehyo@DESKTOP-UID0111:~/gitclone/widehyo1.github.io/_posts/interest$ cat /mnt/c/Users/widehyo/.wezterm.lua
-- Pull in the wezterm API
local wezterm = require 'wezterm'

-- This will hold the configuration.
local config = wezterm.config_builder()

-- This is where you actually apply your config choices.
config.default_prog = { 'powershell.exe' }
-- WSL을 메뉴로 추가
config.launch_menu = {
  {
    label = "WSL: Ubuntu",
    args = { "wsl.exe", "--cd", "~" },
  },
}

config.keys = {
    {
        key = "w",
        mods = "ALT",
        action = wezterm.action.SpawnCommandInNewTab {
            args = { "wsl.exe", "--cd", "~" },
        },
    },
}

-- For example, changing the initial geometry for new windows:
config.initial_cols = 120
config.initial_rows = 28

-- or, changing the font size and color scheme.
config.font_size = 10
config.color_scheme = 'AdventureTime'

-- Finally, return the configuration to wezterm:
return config
```

```
widehyo@DESKTOP-UID0111:~/gitclone/widehyo1.github.io/_posts/interest$ cat /mnt/c/Users/widehyo/Documents/WindowsPowerSh
ell/Microsoft.PowerShell_profile.ps1
function img {
  param(
    [string]$path
  )
  & "C:\Program Files\WezTerm\wezterm.exe" imgcat $path
}
```
